// Importing createContext api from react 
import { createContext } from "react";

// Create context with the name of noteContext
const noteContext = createContext();

// Exporting the noteContext
export default noteContext;